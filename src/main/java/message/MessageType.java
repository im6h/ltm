package message;

public enum MessageType {
    LOGIN,
    MESSAGE,
    LIST_USER,
    LIST_ROOM,
    JOIN_ROOM,
    GET_MESSAGE,
    SHOW_CAMERA,
    SHOW_URL,
    RESPONSE_MESSAGE,
    RESPONSE_FILE
}
